[project]
name = "heybox-video"
version = "0.1.0"
description = "小黑盒帖子视频直链解析"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "nodriver>=0.50.3",
    "playwright>=1.60.0",
    "requests",
    "uvicorn>=0.49.0",
]

[project.scripts]
start = "main:main"
